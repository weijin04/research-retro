# Cold A — independent first evaluation

Pass for a bounded scientific takeover, with one factual wording correction. A new reader used the package to perform a new invariance check and change the next decision; it did not merely restate the previous all-min/path problem.

Evaluator read the audit code, captured geometry definitions and CoNi extension, source network, results and record request. It independently replayed the unchanged audit in evaluator scratch: numerical JSON matches except timestamps; comparison CSV is byte-identical; all delivery hashes match. Five source blobs are explicitly resolved inside the package and hash checked. Reported absolute source names are metadata, not audit read targets. No complete shell transcript exists, so absence of every possible outside read is not independently proven.

The 2410 comparisons cover ten specified frames, all 42 windows and nonidentity cyclic permutations; 1308 changes exceed 1e-6 A, 36 windows affected, maximum 3.8966857296 A. Recalibration per ordering matters: static equality is built into the formula, so it cannot certify that dynamic center identity is preserved. The result is consequential for interpreting the archived estimator and for deciding whether full-path statistics are meaningful. It does not identify which ordering/center is physical, prove original ordered input invalid, or show real opening/selectivity. The report generally states these boundaries correctly. It also correctly avoids treating sampled frames as probability estimates or matching symbols as full atom-identity proof.

Correction: the captured network does contain explicit center_frac, including w0016 [0.5, 0.01838390702576762, 0.9999230885443133]. The report's wording about no geometric-center anchor is too broad. What is absent from the tested calculation is use/validation of that available reference with explicit per-atom periodic images and continuous tracking. A useful next action should validate existing center data rather than assume reference centers must be invented. This qualification leaves the reproducible permutation counterexample intact.

Initial failure preserved: author log records H=1.20 versus actual H=1.09 assertion failure before trajectory processing, followed by correction and successful run. Do not label this uninterrupted. The report accurately acknowledges incomplete terminal logging. Later evaluation replay succeeded immediately but is not the original run.

Two new scientific records (obs:permutation and route:identity) and their dependency are in the submitted record request; the delivery receipt identifies transaction and state revision. Product-level conclusion is limited to useful cold continuation from repaired packaging of unchanged A science. It does not establish full-project completeness, general unseen performance, causal Jev value or isolated runtime safety. Stage B remains unreviewed.
