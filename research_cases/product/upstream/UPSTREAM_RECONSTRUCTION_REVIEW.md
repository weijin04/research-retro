# 前置候选链后的有限重建：独立评价

结论：本次确实形成“便宜候选 → Jev局部关系 → 强宿主选择 → 原件复算 → 科学节点/Spine”的有限闭环。它证明该信息流可运行并产生有用的小段重建，尚不证明Jev带来额外发现、总体提速或全项目覆盖。

## 链条与读集

dsh三批最终产生30/20/40项，A批首次启动配置失败后重试，原失败保留。90条Jev回执时间为18:29:21–18:32:51 UTC；导出SQLite第一条research_node是18:40:55，确认判断前没有科学节点。强宿主声明只看前15项，选c14/c15/c07；读迹首先打开R4收轮、R3总结、run2 brief、原brief，与选题逐项吻合，再向CSV/JSON/代码/网络追索。

读迹可复算为17个唯一科研原件，其中15个进入模型文字；16次文字阅读合79450 UTF-8 bytes（含重读），程序输入810572 bytes（含重读），exact tokens unknown。它不包括队列、协议、导航或上游worker/Jev开销，不能拿此数作为总成本。read-trace是宿主记账，无逐次时间戳和强制访问隔离；可证路径/内容与核查一致，不能独立证明没有其它未记读取。

## 候选提供什么，宿主补出什么

候选先提供了run1输入错误/VOID、run2止损及CALF装载负结果三条入口。宿主没有仅将它们改写成节点：独立整数平移图检查得到HKUST全窗min=0.0333747、周期阈值3.9208及下一更高阈值无绕行的见证；重计run2为0/9、2/13、18/23；重算三CSV零事件和装载中位差。

新增承重联系是R4 CALF row4直接使用R3 guest-free的动态q95、static_only=false，因此这两轮并非独立静态/动态证据。独立检查前15项的title/target，未见这一确切联系；只能说超出这15项，不能声称全90项/全历史首次发现。候选中已含CALF/CoNi校准线索，不能把全部校准洞察都记成无提示原创。

主线保留正确停止R4规则，同时拒绝所有几何/客体开门普遍被否定，选择先核CALF少量既有帧的观察算子。大offset不自动是错误、相关帧不作独立样本、分口径率差不作因果效应、约208K仅是继承报告，这些范围限制恰当。未做坐标几何/温度/势或实验标签的独立再核，不应给更广物理资格。

## Jev误差如何被处理

c14核心经实际证据验证。c15的stale没有被机械解释为删除仍约束后续动作的止损。c07的counters没有被升级为所有guest-induced opening被证伪，而收窄到此模型/轨迹/代理。另准备了reuse genealogy和scope两问题，尚未提交，不能算已完成第二轮判断。

本次处理表明强宿主确有审查/补充，不证明Jev是产生这些修正的必要原因。刚完成的12项局部审查未给宿主反馈；它仅作独立事后对照，不把样本中的别项修复记为本宿主成绩。一般化问题文字/截断元数据即将修复，不能回改旧90项成绩。

## 原件、程序与状态实核

冻结包目录名reasoner-r225，实际状态r227；136个manifest项hash全匹配。read-trace记录的全部原件hash与A匹配。将verify.py复制到evaluator scratch，仅把输入根S显式重定向到原冻结A，所有运算不变，重放verification.json完全一致，未修改原workdir或原读迹。

最终有11科研节点；Spine使用已核证据与有限范围，next不宣称后续几何检查已做。初次claim:void/claim:boundednegative直接source仅历史文本，产品给history_only提示；宿主随后显式补主数据并重绑next依赖，旧回执与源绑定修复保留。这是初次工作内部的可追溯修正，不是外部揭示科学答案。未把这次修复伪装成初始无问题记录。

## 可以与不可以声称

可声称：先前缺失的前置候选信息流本次真实执行；少量重点原件读取和程序复算产生一段可继续工作的科研结构，宿主能修正宽断言并发现候选未表达的关联。

不可声称：完整43596文件项目重建；队列召回率；Jev优于只看worker候选；总时间/成本下降；比旧非随机非等预算三臂更好。没有同范围匹配对照，样本也已有策展与历史说明。Luna预试排除。当前新冷读者未接触、未查看，不预判其结果。
