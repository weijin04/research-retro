# 2. 完善可用的 AI 复盘模块

## 2.1 使用者必须能完成的工作

接入一个历史项目后，使用者可以询问：某个结论真实来源是什么；它实际检验了哪个命题；同名结果是否可比较；某路线被停止时究竟否定了什么；人类纠正后来是否进入代码、状态和报告；还有哪些无归属结果可能改变理解；接手 agent 应该相信什么、回到哪里核验。

复盘必须恢复五个不同对象：**实际发生的工作、AI 当时相信的内容、AI 对外报告的内容、后续记忆保存的内容、今天根据证据可采信的内容**。缺少当时记录时，只能标记未知，不能用今天的解释填补历史。

## 2.2 首个开发项目及迁移原则

默认选 grephene 为开发/试用项目，原因是已存在 census、evidence map、原始输入输出与多轮审阅，适合检验“复用历史资产但不继承未经审计结论”的能力。入口已经在本轮只读确认，见来源说明 L03、L04。

先导入已有索引与解析结果，保持原字段、原判定、生成时间、原脚本版本及来源标识。对实际可读原件进行指纹/字段抽查，复核承重条目；然后扫描旧索引范围以外和发生变化的部分。出现解析器系统性错误时，对受影响数据族定向重解析。

不能把旧 `RAW-OK`、目录名或 README 声明直接换成新系统的“已审通过”。导入状态是 `imported_assertion`，它保留历史价值；只有满足当前核验条件的条目才获得当前资格。

R2 后期使用 diffusion 的一个独立切片检查跨领域契约。该切片是兼容性开发材料。不要把开发过的数据重新称为盲测。

## 2.3 工作链及可逆迭代

### A. 授权与接入

创建 ProjectContract 与 SourceManifest：范围、只读源、输出位置、禁止动作、外发策略、解析预算和验收目标。

首次接入只读取指定源；不得扫描整个 home、密钥目录或所有 agent 私有历史。用户授权的对话导出可以接入，未提供的历史不要推测存在。

来源类型第一版覆盖本地目录、git 元数据、CSV/JSON/TSV/Markdown/文本、代码、结构化计算输入输出和已有索引。PDF、DOCX、谱图、数据库、轨迹等依真实项目需要接入现有读取器。涉及图像/图表的承重内容应保留原图定位并进行适当视觉核验，无法提取时显式保留阅读缺口。

压缩包默认只列目录和元数据；确需展开时限制路径穿越、符号链接、展开体积和数量。解析外部文件不执行其中代码。

### B. 覆盖清点与内容固定

所有发现的资产登记处理状态：待读、已读、已解析、部分解析、格式不支持、读取失败、正在变化、不在范围。保存范围分母和排除原因。

不要首次读入全部大轨迹。先元数据清点；对承重文件保留完整内容指纹或经说明的稳定片段；对大文件采用分块/分段读取，并显式注明尚未固定的部分。相同 size/mtime 只能用于候选变更检测，不能证明内容身份。

承重数据使用独立副本、受支持的快照或版本化对象。指向可变原件的符号链接与硬链接不能充当不可变快照。读取变化文件时保存读取区间和前后元数据，必要时复读。

可增量恢复的检查点以 source/asset/parser version 为单位，禁止每次失败重新清点全部项目。

### C. 还原实际实验/运行

从输入引用、作业 ID、脚本调用、文件内容、git 记录和输出日志建立 Run。关联不确定时记录候选，不能靠同一目录强行拼成一组。

区分：意图协议、生成的输入、实际执行的输入、实际执行版本、原始输出、后处理版本、报告。当前工作树不能替代历史版本；历史版本缺失时保留缺口。

Run 的多轴状态至少包括：进程完成、协议一致、数值健康/收敛、方法适用、科学检验有效性。正常退出不能填满其余轴。

代码侧检查对象包括未调用函数、mock/fast 路径、失败后替代实现、默认参数覆盖、单位转换、错误目标量、泄漏、重复样本、调参与评测数据混用。无需先给全仓代码打分，围绕实际生成承重结果的调用链检查。

### D. 保留对象差异与可比性

对象识别同时保存原始名称、规范候选和显式条件。化学例子涉及组成、电荷、自旋、几何、环境、方法、能量类型、参考态和约束；扩散例子涉及材料/客体、温度、载量、刚柔、时间窗、扩散定义和采样方式。

检验“是否同一对象”“是否同一次运行”“是否可作数值比较”“是否支持同一命题”时使用不同关系。不得用一个相似度阈值同时处理四件事。

Jev 提出别名和关系候选；明确数值、组成和方法约束由程序/领域工具检查。合并采用可撤销的别名关系或等价集版本，保留原对象 ID 与来源。未知条件阻止无条件合并，必要时保留条件性比较。

### E. 恢复历史主张与分支

按当时可用证据建立 HistoricalAssertion 和 Branch。区分用户要求、模型假设、结果报告、计划、审阅和后续记忆；原文与精确定位保留。

时间至少包含：事件发生时间、记录时间、系统采集时间；缺失或时区不明显式保存。上传时间和文件 mtime 不替代科学事件时间。

分支按问题/机制/方法身份建立，不按目录编号或当前路线图套用。一个历史分支可以拆分为多个适用范围，一个现在的分支也可以由不同历史来源组成。保留共同祖先和旧名称。

### F. 深审工作台：本模块的科学核心

系统把一个需要实际解决的承重关系形成 AuditCase，并向研究 agent 提供证据包和工具入口。这个 agent 可以自主检索、推导、写局部检查、读取更多原件、提出新表示；在复盘授权内推进到实质判定。

每个 AuditCase 必须输出：

1. 原始主张与真实要判断的问题，二者有何差别；
2. 相关对象/目标量/条件与关键原件；
3. 对已有结果的复算、推导、执行路径核查或科学比较；
4. 最有力的竞争解释或反例及其处理；
5. 当前支持范围、最先失效条件、残余有效资产；
6. 哪些主张、分支、任务与记忆需变化。

如果材料支持某项困难推导，就完成该推导；不能仅生成“建议进一步核验”的任务。新科学计算确实必要时，完成不依赖新计算的全部部分，再列出精确缺口和可区分的新任务。

审计选择同时覆盖承重主张、可疑来源家族、未归属资产、已淡出记录的矛盾。可分批执行，但不能预设“审四个 family”就宣称全项目复盘完整。

### G. 裁决与负知识

证据资格和工作处置分开。

证据判定：支持、有条件支持、遭反对、已在限定范围反驳、检验无效、依据不足、尚未检验、混合/争议。

工作处置：活跃、暂存、成本搁置、等待输入、需重开、停止。支持但搁置、检验无效但值得重开均是合法组合。

负知识保存：被否定的命题、适用范围、证据、失效环节、仍成立部分、重开条件。失败类型至少可区分表示缺失、候选未生成、搜索未到达、排序错误、参考问题、实现问题与科学反证；分类后继续给出相应修复或现有证据下的替代解释。

### H. 综合、交接与持续更新

综合的主线围绕项目目标、关键对象、已建立关系、竞争解释和未解关系。历史时间线只是辅助视图。

发布时生成：当前状态版本、来源覆盖、关键 AuditCase、命题/推断/负知识账本、可复用资产、纠偏差异和可回溯交接包。

源项目之后继续变化时，重新接入差异并失效相关结论。只补充了新数据不会自动改变所有旧审计；只修改了解释也不会让原始结果消失。

## 2.4 一个必须贯通的合成案例

本例只用于软件行为验证，不是用户项目的真实结论。

历史记录把两个端点的电子能差写成反应势垒；另一份报告复制了这一说法；一个独立产物结构结果与此没有逻辑依赖。

产品应识别两个报告共享同一来源，恢复端点能差这个真实目标量，判断端点差不足以提供完整路径势垒，并撤销这条动力学推断的支持资格。依赖它的“室温主通量占优”主张失去该支持；独立产物结构结果保留。对未经过渡态计算的路径，只能记录相应未知，不能反向声称该机制不可能。

该案例贯通读取、对象条件、来源去重、推断审查、影响传播、报告重建。随后用真实开发项目的实际 AuditCase 替换示例。不得只完成合成案例就发布 R2。

## 2.5 产品交付视图

| 视图 | 内容与核验要求 |
|---|---|
| 范围/覆盖表 | 源快照、资产分母、处理状态、遗漏和访问限制 |
| 对象/实际工作表 | 对象条件、输入/版本/输出、运行多轴状态、来源定位 |
| 主张/推断表 | 历史原话、当前精确命题、条件、支持集合、反证、审计 |
| 分支/负知识表 | 当时意义、现在证据资格、工作处置、失效范围与重开条件 |
| 矛盾/修订表 | 冲突对象、可比性、处理依据、失效与保留部分 |
| 可复用能力表 | 代码/数据/结构/方法的输入输出、前置条件、实测能力与失败边界 |
| 项目科学地图 | 目标、表示、必要关系、竞争模型、孤立证据、关键缺口 |
| 交接包 | 任务无关的稳定底稿、当前版本、关键入口、必要约束和恢复说明 |

这些是信息覆盖要求。文件类型和页面布局可自主选择，避免复制八份会各自漂移的手工表。

## 2.6 R2 发布资格

针对声明的授权范围，资产有完整处理记录；当前报告所有承重主张具有来源与推断记录，或已经明确降级；科学关键关系完成深审，未以清点替代；发现的分支与孤立资产有去向；可以续跑与增量更新；能从一次纠偏得到真正改变的上下文和报告；另一个没有读过仓库的 agent 可以依交接包回答项目关键问题并回原件核验。

不要求所有历史文件均被高成本模型细读，也不要求项目科研问题全部解决。需要完整说明哪些科学问题已解决、哪些在已完成审计后仍未知、哪些因实际外部阻塞未完成。三种情况不得混写。
