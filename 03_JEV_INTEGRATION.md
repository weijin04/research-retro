# 3. Jev：建设期使用与产品集成

## 3.1 已确认的本机入口

已读取：

`/home/sun07ao/.claude/plugins/cache/typesafe-ai/typesafe/0.5.7/skills/typesafe-ai/SKILL.md`

该插件 README 给出的显式调用为 `/typesafe:typesafe-ai`。[L02] `0.5.7` 是插件版本，不能称为 Jev 模型版本。具体 SDK、密钥获取和实际模型响应需在本机做预检。优先复用用户已经可用的配置，不读取或输出密钥内容，不重新安装全局环境。

官方 API 接收 `state`、`model`、`questions`；支持 `Noul`、`Choice`、`Score`，返回答案及 usage，响应中的 model 字段用于记录实际版本。[S01][S02] Python SDK 有同步/异步客户端，环境变量入口为 `TYPESAFE_API_KEY`。[S03]

建设 agent 先阅读本地 skill，再按当前官方文档检查适配器。不要在本包里固化一个以后可能过时的 CLI flag。

## 3.2 建设期如何立即使用

R0 进行一次不含私密项目内容的实际 API smoke：用 `examples/jev_preflight_request.json` 的合成文字，确认请求、三类返回、模型版本、usage 和失败处理。官方文档允许同一状态上的独立问题并行执行；题目不能引用同一批另一题尚未产生的答案。[S01]

随后可以在框架自己的文档、测试样例或已批准的去敏片段上使用 Jev：

- 给开发材料加内容标签，帮助定位接口、结果、假设和未完成工作。
- 对候选引用或候选关系做筛查，辅助定位需要深读的位置。
- 比较报告中的主张与指定证据片段，发现明显的条件遗漏。
- 对任务提案做类型路由和疑似重复提示。

这些调用全部走同一 JevAdapter，记录版本和实际成本。临时试验结果可以导出为开发样例。不要散落多个不记账的脚本调用，不把建设 agent 的自查当独立评测。

当前官方模型页标明文本输入，不能直接把谱图、PDF 图像或轨迹二进制当作 Jev state；应由适当读取器/视觉模型获得带原图定位的候选描述，再对文本做窄判断。[S08] 中文科研材料还应做少量实际代表性边界检查：可以使用英文题目配中文原文，不默认把全部证据先翻译成英文。翻译或提取产物保留原文与转换来源。完整跨语言校准仍放到 E1。

## 3.3 集成接口

建议内部接口：

`evaluate(task_family, state_ref, questions_version, policy_version, requested_model, egress_scope) -> DecisionRecord`

DecisionRecord 至少保存请求内容指纹、具体题目与候选、模型请求/返回版本、原始分布、可空 confidence、usage、耗时、缓存命中、重试、失败类型、最终采取动作、后续核验结果。

缓存键包含内容身份、题目定义、候选集、模型实际版本/别名解析策略和策略版本。改变条件、候选或模型后禁止沿用旧判定。别名版本无法确认时不宣称跨时段可重现；模型响应与题目版本必须保留。

服务失败与语义不确定是不同状态。超时、鉴权、限流、返回不合约分别登记；有界退避，父任务总重试/预算不因创建子任务重置。

## 3.4 产品内的题目族

| 题目族 | 何时调用 | 所需最小状态 | 可以影响什么 |
|---|---|---|---|
| document_role | 文本切片后 | 原文及少量上下文 | 候选标签、解析/深读队列 |
| candidate_relation | 代码产生候选后 | 两条记录及各自条件 | 待审关系；不能直接永久合并对象 |
| evidence_fit | 已给出主张和来源后 | 命题、限定、精确原文 | 明显超范围提示；不能证明新机制 |
| correction_link | 修改后补充隐含依赖 | 修改前后、候选条目、关键定义 | 扩大待核验影响集 |
| task_route | 行动申请时 | 目标、当前问题、提案、既有类似任务 | 处理通道与需要的检查 |
| context_role | 编译上下文时 | 当前问题、片段、对象范围 | 排序/加标签，不删除必带反证和纠正 |

题目定义初稿见 `examples/jev_questions.json`。采用明确的 `other`、`insufficient_context` 等候选，不把分类器暂时无法表达的内容丢掉。新关系/机制由生成模型提出，随后扩展候选或 schema。

题目 ID 只供代码匹配，官方 API 不将 ID 当题意输入。因此所有语义必须写在 instructions/criteria 中。[S02]

## 3.5 权限演进

**R0/R1：记录与建议。** Jev 的误判不能导致删除资产、取消审计、改写科学状态或发布结论。实际连通性和若干代表性边界检查通过后，可自动加标签、生成候选队列。

**R2：有限低风险自动化。** 可以调整阅读顺序、复用已经核验的重复判定、路由机械子任务。所有被排后或排除的材料保留可恢复入口；必带来源和审计覆盖不受其裁决。

**F1：参与控制决策。** Jev 可以提出需要更多审查、任务类型或语义依赖，但真正执行许可由权限、输入版本、任务合同和资源租约决定。科学修订走受检提交。

**E1：根据分任务评测决定权限是否扩大。** 不预设某个 confidence 即代表科学安全。自动化权限可以保持不变、缩小或撤回。

官方说明 confidence 由分布计算，Noul 没有独立 confidence；不同 primitive 的阈值不可直接互换。数值运算、多跳判断、长无关状态与对抗内容属于需要特别处理的范围。[S04][S06]

## 3.6 实际组合逻辑

每一步先由程序保证候选来源与精确定位，再由 Jev 处理窄语义，之后由代码或研究 agent 处理后果。

例：

```text
程序发现报告引用了 output-A 的某段
  → 程序取出准确段落与命题条件
  → Jev 分类为“只支持更窄主张”
  → 创建 AuditCase，保留原判断与片段
  → 研究 agent 检验目标量/前提/推导
  → 提交带依据的科学修订
  → 确定性事务更新支持与下游视图
```

对“两个值相差多少”“是否同一 hash”“哪些节点可达”直接计算。Jev 不负责替代计算器或图算法。对于多条来源之间的复杂科学推断，不把复杂性藏进一个“整体正确吗”的题目。

## 3.7 数据外发与可用性

本机可调用 Jev 不等于数据留在本机。实际 endpoint 是本地代理或外部 API，必须记录。对项目数据应用 source-specific 外发范围，只发送完成该判断所需片段，去掉凭证和不相关个人信息。未经允许的片段改走本地处理或已授权的生成模型通道。

Jev 不可用时：原件保存、确定性解析、已授权的低风险工作继续；需要语义审查的操作进入可恢复队列。只有缺乏依据的采信或风险动作被限制，不使整个项目停摆。
