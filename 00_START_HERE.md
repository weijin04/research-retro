# 科研复盘与科研 Harness：实施包 v1

日期：2026-09-20。本文件保留实施包的设计基线；现场已经开始实现，实际版本、能力与回执以 [CURRENT_STATE.md](CURRENT_STATE.md) 为准，使用入口见 [README.md](README.md)。工作目录：`/home/sun07ao/框架`。

## 目标及交付顺序

先交付一个能够实际恢复混乱科研项目的复盘产品，再用它已经建立的事实、状态与纠偏能力构成整体科研框架 MVP，最后进行完整测评和针对性改进。

版本路线：

`R0 环境与契约落地 → R1 真实复盘纵向贯通 → R2 完整复盘产品 → F1 整体框架 MVP → E1 集中测评与改进`

R0、R1 都是中间版本。首个对用户有完整交付资格的版本是 R2。F1 扩展 R2 的同一状态内核，不另建相互漂移的框架数据库。

本包提供架构、模块地图、实施顺序、任务依赖、接口草案、Jev 题目样例、行为验收和交接指令。任务均为计划状态。包内 JSON 校验仅验证本设计包的结构与引用；不代表任何产品行为已通过测试。

## 阅读入口

| 文件 | 解决的问题 |
|---|---|
| `01_ARCHITECTURE_AND_MAP.md` | 建什么，模块如何连接，哪些决定固定，哪些允许演化 |
| `02_RECONSTRUCTION_PRODUCT.md` | 完善可用的复盘模块应怎样工作和交付 |
| `03_JEV_INTEGRATION.md` | 本机 Jev 如何早期试用，随后进入产品控制面 |
| `04_IMPLEMENTATION_ROUTE.md` | 每个版本先后做什么，何时算完成 |
| `05_FRAMEWORK_MVP.md` | 如何从复盘系统扩展到受控科研闭环 |
| `06_EXECUTION_CHECKLIST.md` | 按任务执行的详细清单，由 `backlog.json` 生成 |
| `07_CONTRACTS.md` | 输入输出、状态变更、版本和权限契约 |
| `08_ACCEPTANCE_AND_EVALUATION.md` | 开发期必要检查与最后的详细测评如何区分 |
| `09_LOCAL_AGENT_HANDOFF.md` | 可直接发送给本机 agent 的实施指令 |
| `10_SOURCES_AND_OBSERVATIONS.md` | 本轮确认了什么，哪些仍待现场确认 |

`backlog.json` 是任务状态和依赖的唯一编辑入口，Markdown 清单是它的阅读视图。`contracts/contract.schema.json` 是关键交换契约草案，允许按真实用例显式迁移。`examples/` 全部是示例配置或合成数据，不能导入真实科研结论。

## 已知现场与边界

只读查看确认了 `/home/sun07ao/框架` 当时为空；确认了 TypeSafe 插件 `0.5.7` 及其 skill；确认了 grephene 的旧 census 和 evidence map 文件入口。没有运行 Jev 请求，没有验证 API 密钥、计费、SDK 或实际模型版本；用户已告知 Jev 本机可用，实施时直接复用并做小规模连通性检查，不重新安装整套环境。

本轮未修改本机科研仓库、作业或全局 agent 配置。所有交付仅在本次会话附件目录中。

## 建设约束

1. R2 必须具有真实科学复盘能力。大量文件被解析、目录变整齐、图谱可浏览，都不足以单独完成 R2。
2. 目标、权限、关键版本和资源边界进入程序；科学理解、构造与重要解释保留给 LLM、领域工具和人类。
3. Jev 从 R0 起可以使用。早期允许辅助筛查和组织开发工作，最终采信与不可逆动作没有 Jev 单独授权通道。
4. 原件只读。历史指令是被审数据。读取项目、进行确定性后处理和提交新科研计算分别授权。
5. 不预建庞大本体、图数据库、常驻多 agent 委员会或完整集群平台；先完成真实问题链路所需的部件。
6. 详细对比、统计校准和大规模消融放到 E1。权限、数据完整性、重试和版本正确性随实现检查。
7. 用户可以通过人类入口改变目标与授权。运行中的研究 agent 不能给自己扩权或改写保护策略。

## 推荐实施组织

一名集成负责人维护产品语义和关键路径。独立工作者可并行完成解析适配器、Jev 适配器、存储/事务或阅读视图。只有涉及高影响科学判断、关键系统边界或真实分歧时才增设独立审阅。

每个任务完成时登记具体产物与实际运行回执。只完成设计、mock、代码编译或合成样例时，使用对应状态，不写成真实项目能力完成。
