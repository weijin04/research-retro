# 8. 开发验收与最终测评分离

## 8.1 两类工作

开发阶段执行保证软件真实可用的检查：接口与版本、读取完整性、权限、幂等、恢复、基本科学语义、少量真实用例贯通。这些检查随任务进行。

E1 才开展完整模型/系统对比、统计校准、大规模鲁棒性、成本收益、消融和跨项目评估。建设过程记录足够回执，届时无需重造日志。

测评资源不能替代 R2 的产品建设；“测评最后”也不能用来延期源数据保护和事务正确性。

## 8.2 R0 必要检查

确认项目独立环境，git 根未误指向整个 home；不输出密钥；真实 Jev smoke 有回执；三个 primitive 的解析正确；服务不可用时保留正确故障状态；不得把合成返回写成 live。

## 8.3 R1 必要检查

一个真实 AuditCase 能从原件贯通到科学判定与报告。引用定位与文件版本一致。进程正常退出不会自动产生“科学有效”。重复副本不增加独立证据数。撤销一个推断前提后，相关报告与依赖失效，独立证据保留。

## 8.4 R2 发布检查

| 场景 | 必须观察到的行为 |
|---|---|
| 旧索引导入 | 历史判定保留，未经复核不升级为当前真相 |
| 源在读取时变化 | 返回变化状态或稳定快照，禁止悄悄拼接不同版本 |
| 同名不同条件 | 保留必要身份差异，限制不可比数值的合并 |
| 原始输出与报告矛盾 | 两者分别记录，完成目标量/协议/推断的实质审计 |
| 来源副本与循环引用 | 不增加独立支持；能显示共同祖先 |
| 未归属结果 | 有可查询队列与处理结果，不从报告中消失 |
| 实现失败 | 不直接等同科学反证；残余有效资产仍可找回 |
| 前提撤销且存在替代支持 | 只撤销对应支持集合，保留仍成立部分 |
| 无剩余支持 | 结论降为无当前支持，不能自动推出相反结论 |
| 纠偏中断 | 恢复后状态和失效通知一致，无半更新项目 |
| 增量重跑 | 未变资产不重复耗费全量解析；变化依赖重新审查 |
| 历史指令注入 | 保存为来源数据，不能触发续算/删除/扩权 |
| 外发范围不允许 | 敏感片段不进入 Jev 请求，其他工作继续 |
| 新 agent 接手 | 能解释目标、关键关系、条件、负知识，并查回原件 |

每条检查保存输入、动作、真实输出和检查脚本/审阅记录。有限用例通过只支持这些用例和相应部署范围，不宣称已证明所有未知情况安全。

## 8.5 F1 必要检查

worker 无法修改原件、权威数据库、broker 策略或预算；未获权限的远端工具不可调用。相关 read-set 变化阻止旧行动未经重评启动，无关更新不阻塞。网络断开造成 dispatch_unknown 时不盲目重发。子任务花费累计到父预算。Jev 故障可以回退。新表示可以进入探索任务。一次人类纠正真正影响新上下文和下一步行动。

对硬控制应实施实际越界尝试，观察操作被拒绝和拒绝回执。只阅读权限配置或审查代码，不能称为行为测试。不可在真实科研源上做破坏性测试，使用隔离副本和临时资源。

## 8.6 E1 测评设计

### 数据层

开发用的 grephene 及 diffusion 切片明确标记为 dev。E1 开始时建立版本冻结的数据清单，区分历史回放、独立项目、自然新增数据与合成故障。

独立性以来源家族和形成时间检查，不能只随机拆文件。复制结果、相同原始运行派生的多个报告、相同模型/参数扫描共享条件，都需要记录关联。接触过答案的数据可以作回归，不能作独立盲证据。

由原件、确定性重算和适当专家复核构造参考；专家分歧保留。另一模型的输出不能单独作为科学真值。

### 任务层

评估资产恢复、运行还原、对象区分、主张与推断审计、分支/负知识恢复、纠偏传播、报告条件保真、接手成功，以及研究模式下的执行与认识更新。各维度分别报告。

### 对照层

- A：相同生成模型、普通文件/总结工作流。
- B：同一模型 + 确定性状态/来源/权限 Harness，关闭 Jev。
- C：B + Jev 语义判定。
- D：C + 事件触发独立重建/审查。

对照应具有相同目标、可用原始证据与声明的资源预算；上下文选择本身属于处理差异，完整记录而非悄悄给某组更多人工整理材料。既记录等预算表现，也记录达到同等产物资格的实际成本。数据顺序和随机性可做重复与分组，不把固定一次 lucky run 当稳定能力。

### 指标层

| 指标 | 建议定义 |
|---|---|
| 承重错误采信 | 不获证据许可却进入正式结论的承重主张数/被审承重主张数 |
| 条件丢失 | 原证据必要条件未进入结论或交接的比例，按条件类型分层 |
| 对象误并/漏并 | 与来源及操作等价性参考对照，分别统计 |
| 来源独立性错误 | 把副本或共祖来源误记为独立支持的次数 |
| 分支与负知识恢复 | 被发现、正确解释、仍保留范围的分支数量与遗漏 |
| 纠偏传播 | 应影响对象的召回、误伤、传播后实际行为符合度 |
| 科学深审完成 | 困难关系是否完成要求的推导/核验；不能以表格数量替代 |
| 接手质量 | 新 agent 的项目理解、证据查询、有效约束保留及错误继承 |
| 资源与运行 | 真实 token/API usage、工具/计算消耗、延迟、失败与重试、人工介入 |
| 开放性代价 | 被错误阻止的合法新候选/表示，以及失败后是否能重开 |

不能将所有维度压成一个总分。成本降低以相应产物质量保持为条件；自动通过率必须同时给出误放行和误阻塞。

### Jev 专项

按题目族分开检查概率/置信度、错误类型与操作后果。包括中文、混合语言、否定、条件省略、跨版本别名、多个正确候选、候选均不合适和注入内容。

画出选择性覆盖与错误关系，并在校准集确定阈值、独立集报告表现。模型/题目/候选改变后重新核验。Noul、Choice、Score 不能共用未经验证的阈值。

详细记录 candidate generator 的漏召回；答案不在候选中时，重排无济于事，必须修改上游。

## 8.7 修复闭环

发现失败后保留最早失败条件和残余有效部分，修改对应层，并回放受影响案例与未参与修复的数据。没有证据表明某复杂模块有价值时可简化或移除。最终输出能支持哪些部署范围，哪些功能应降权或保持人工/深审介入。

## 8.8 本设计包的检查边界

`checks/validate_plan.py` 仅检查任务 ID/依赖、版本顺序、schema 与合成示例、题目结构和文件引用。它不能检查实际 Jev 准确率、真实项目复盘质量、权限隔离或软件运行行为。本轮交付不会把这些待实施能力标成已通过。
