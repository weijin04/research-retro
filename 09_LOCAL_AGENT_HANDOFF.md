# 可直接发给本机 agent 的实施指令

你负责在本机实现“AI 复盘冗杂项目模块”及其后续科研 Harness。以这份实施包为设计起点，先完整阅读 `00_START_HERE.md`、`01_ARCHITECTURE_AND_MAP.md`、`02_RECONSTRUCTION_PRODUCT.md` 和 `04_IMPLEMENTATION_ROUTE.md`，再按当前阶段读取 Jev、契约、执行清单和 MVP 文档。

## 目标与优先级

先交付一个完善可用的复盘产品 R2。它必须从真实混乱资产恢复实际工作，区分对象与条件，完成承重科学关系的审计，恢复有效/无效/未知分支与负知识，并生成能够可靠接手的项目状态。R0/R1 仅是建设中间结果。R2 之后扩展整体框架 MVP F1，最后开展 E1 的系统测评与定向改进。

不要把完成清点、搭好数据库、做出知识图或生成报告等同于完成复盘。遇到决定结论的关系，应在授权材料内完成所需推导、复算、代码路径检查和竞争解释比较。现有材料能解决的部分先解决，缺失原件或新科研计算需求精确记录。

## 本机入口与复用

建议项目目录为 `/home/sun07ao/框架`，本轮设计时只读查看为空。开始时重新检查，保留后来新增的内容。确认 git 根目录，避免对 home 仓库进行大范围修改。

用户已说明 Jev 本机可用。已经找到 TypeSafe 插件 skill：

`/home/sun07ao/.claude/plugins/cache/typesafe-ai/typesafe/0.5.7/skills/typesafe-ai/SKILL.md`

先读它并使用现有配置；不要重新安装全局环境或输出密钥。以合成/公开数据进行小规模真实调用，保存实际模型、usage、错误和返回。开发初期就可使用 Jev 分类、筛查、引用审计和任务路由，所有调用统一走可记录的适配器。

默认首个开发项目是 `/home/sun07ao/grephene`。复用以下索引和原始证据入口：

`research/raw_asset_census_20260915/`

`research/scientific_evidence_map_20260915/`

旧总结、旧判定和审阅结果作为来源与线索导入；承重结论回原件。先迁移并做增量补全，不重复手工全盘 census。后续以 diffusion 的独立切片检查跨领域契约；两者均标记为开发/试用材料。

## 授权边界

可以在框架项目内实现代码、测试、示例、运行数据和文档，使用已允许的依赖与现有服务做必要开发检查。科学来源只读；后处理在独立工作区。不要修改科研原件、启动或取消原有作业、提交新的 DFT/MD/训练/集群任务，或修改全局 cc/codex/SSH/安全配置。确实需要新的授权时定位具体缺口，同时完成不受影响部分。

外发数据遵循来源范围与已有授权。Jev 凭证可用不代表所有科研数据都可外发。先使用合成材料和最小去敏片段，不把整个仓库或用户隐私发送给分类器。

## 实现方式

使用模块化单体和小型共享状态内核。优先真实链路可运行，再增加覆盖与便利性。`backlog.json` 提供任务、依赖和验收，可根据真实代码与证据修改；保留修改理由和对产品范围的影响，不机械执行错误计划。

由一个集成负责人维护语义、版本和关键路径，可并行委派解析、Jev 适配、存储/事务及阅读视图。重要科学裁决或控制边界变化时按需调用独立审阅，不建立永久投票委员会。不要以代理数量代替独立证据。

LLM 保留开放生成、表示重构和求解能力。Jev 只承担窄语义判断，确定性代码维护权限、资源、版本、来源和依赖传播。运行 worker 与建设 agent 的权限不同；F1 必须证明 worker 不能绕过控制面。只有 hooks 的部署应明确标记观察模式。

开发期持续做必要的安全、数据、事务和真实链路检查。详细对比、统计校准和大规模消融后置到 E1。所有“已完成”关联实际产物和真实回执，未运行、仅 mock、合成示例和代码审查分别标明。

## 开始工作及持续推进

先确认现场、建立独立项目、读取 skill、完成真实 Jev smoke，并导入一个可访问的来源切片。接着把一个有科学内容的真实 AuditCase 从原件贯通到推断、修订和报告。完成后继续推进 R2 的覆盖、身份、历史、分支、深审、增量、恢复和交接，不停在 demo。

维护精简的当前状态：当前版本、已落地能力、承重缺口、有效纠正、实际回执和下一组依赖。任何摘要都保留回到来源与结构化状态的入口。修订核心前提时同步更新受影响任务和产物。

每次阶段交付说明实际能做什么、关键科学审计结果、尚存边界，以及如何复现。正常可逆的实现选择自主完成；遇到真正的权限/外部能力缺口才上报。继续实施到各版本声明的可检查产物，详细测评不得提前替代产品建设。
