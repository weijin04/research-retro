# 1. 项目架构与地图

## 1.1 最终产品

系统有两种连续运行模式。

**复盘模式**接收历史混乱资产，恢复对象、实际工作、证据与解释的差别，完成关键科学审计，生成可继续使用的项目状态。

**研究模式**以该状态为起点，让 LLM 主持开放研究，调用领域工具和数值实验，按依据更新判断，接受人类纠正，并把变化传播到下一次行动。

共同内核保存目标、原件、证据、推断、决策与事件。叙述报告、上下文包、路线图和交接文件均为带版本的视图。

## 1.2 模块地图

| 编号 | 模块 | 首次落地 | 输入 → 输出 | 状态权限 |
|---|---|---|---|---|
| K1 | 项目目标与权限清单 | R0 | 用户目标/授权 → 版本化项目合同 | 治理入口修改；worker 只读 |
| K2 | 来源、快照与证据定位 | REC1 | 文件/记录/外部来源 → artifact 和 locator | 接入服务创建，不覆盖原件 |
| K3 | 状态存储与事件事务 | REC1 | 受检补丁 → 新修订、事件、失效通知 | 单一 writer/broker 提交 |
| K4 | 来源/推断依赖与纠偏 | R1 基础，R2 完整 | 证据或前提变更 → 支持重算、影响集 | 确定性传播；语义新增关系待审 |
| K5 | Jev 判定适配器 | R0 | 窄问题与最小相关状态 → 判定回执 | 只写判定记录和候选队列 |
| REC1 | 资产接入与增量清点 | REC1 | 授权源、旧索引 → 覆盖账本 | 对来源只读 |
| REC2 | 解析、对象识别、运行恢复 | R1/R2 | 代码/数据/日志 → 对象和实际运行 | 生成候选事实与核验结果 |
| REC3 | 历史语义与分支恢复 | R1/R2 | 当时记录与事件 → 历史主张、分支、负知识 | 保留原始表述和时间范围 |
| REC4 | 深审工作台与领域核验 | R1/R2 | 承重关系及原件 → 推导/检查/裁决 | 提议科学状态修订 |
| REC5 | 项目综合与交接 | R1/R2 | 已审状态 → 报告、视图、交接包 | 不从自由文本自行制造事实 |
| CTX1 | 上下文编译与按需读取 | R2 基础，F1 扩展 | 任务与当前状态 → 证据上下文包 | 必读项不可被排序删除 |
| LEAD1 | 研究主持与行动提案 | CTX1 | 目标/证据/未知 → 探索、推导、实现、实验任务 | 可创造新候选，不直改权威状态 |
| EXEC1 | 执行 broker 与资源租约 | CTX1 | 任务/权限/输入版本 → 受控执行与回执 | 持有执行凭证与额度 |
| GOV1 | 分支重开、独立审查、人类入口 | R2 局部，F1 完整 | 结构事件或主动提议 → 新上下文/决策 | 人类授权、版本化策略约束 |
| OBS1 | 运行观测、回放与系统测评 | R0 埋点，E1 完整 | 回执/裁决/对照 → 失效分析与改进 | 测评数据与生产状态隔离 |

K 为共享内核，REC 为复盘模块，CTX/LEAD/EXEC/GOV/OBS 为运行模块。版本 R0、R1、R2、F1、E1 与模块 ID 分开。

### 数据流

```text
用户目标与授权                         原始项目与外部证据
      ↓                                      ↓
目标合同 K1                          只读接入 K2 / REC1
      ↓                                      ↓
LLM 科研主持 ← 上下文编译 CTX1 ← 状态内核 K3 ← 解析/运行恢复 REC2
      ↓                            ↑         ↓
任务/科学补丁提案              受检提交入口    历史与分支 REC3
      ↓                            ↑         ↓
Jev 辅助筛查 K5 → broker EXEC1 → 原始输出 → 深审/推导/领域核验 REC4
      ↑                            ↓
纠偏与审查 GOV1 / K4 ← 事件与差异 ← 新版本 → 报告/交接 REC5
```

箭头表示信息或控制流。物理机制图另行建模，不把软件执行顺序写成科学因果链。

## 1.3 第一版的技术选择

采用模块化单体：Python 独立环境、SQLite 元数据、内容寻址的原件/片段存储、CLI 与薄的 agent 工具适配器。需要人类浏览时，提供只读 HTML 导出或轻量浏览页；不先开发复杂前端。

理由是复盘的早期风险集中在数据语义、对象身份、执行还原与推断资格，分布式基础设施不会直接解决这些问题。模块接口独立，可在实际吞吐或多机写入需求出现后替换存储/队列。

数据库保存在本机 Linux 文件系统。选择 WAL 时，各进程必须在同一主机，不能把 SQLite 文件放到跨主机网络文件系统共同写入；远端只通过 broker 或离线回执交换。[S05] 使用单 writer、短事务、显式 checkpoint 与备份恢复方案。关键状态使用要求持久化的事务设置，部署时按实际 SQLite 版本检查。

部署预检要读取 Python 进程实际链接的 SQLite 版本，不能只看系统 sqlite3 命令。官方记录了 WAL-reset 并发问题，修复包含 3.51.3 及后续版本，以及 3.44.6、3.50.7 等回移版本。[S05] 若实际库尚未包含修复，可先用 rollback journal 与单 writer 落地，不为此升级整台科研机器的公共环境。备份必须使用一致性备份方式，不能只复制一个仍有活动 WAL 的数据库主文件。

不把整个系统预先做成事件溯源框架。使用版本化业务表 + 追加事件 + 同事务 outbox，满足修订、回放、重建视图和审计；性能或历史查询需求出现后再扩展。

## 1.4 四种状态及三种关系图

四种状态独立保存：

- 目标/授权：要解决什么、谁可以做什么、哪些要求仍有效。
- 科研状态：对象、观测、计算结果、命题、假设、推断、竞争模型、未知。
- AI 工作状态：任务、实际读取材料、工作上下文、暂时解释、报告与交接。
- 执行状态：代码、输入、运行、资源、输出、退出、协议检查、科学检验资格。

三种图有不同边类型：

1. 来源和证据图：派生、引用、同源、共同前提、支持与反对。
2. 研究对象和机制图：物理/化学/数学对象之间的关系，允许真实反馈与循环。
3. 任务执行图：依赖、父子任务、资源租约、输入输出、取消与重开。

证据循环不会因图遍历而获得新支持。记录各支持集合的原始来源和共同祖先；有向环需要找到外部证据或独立推导才可作为依据。

## 1.5 最小核心对象

| 对象 | 不能丢失的字段 |
|---|---|
| ProjectContract | 目标原文、约束出处、作用范围、权限、预算、可变部分、版本 |
| Artifact/Locator | 来源、内容身份、快照/读取区间、定位方式、读取状态、原件与副本关系 |
| ScientificObject | 对象类别、组成/结构/条件、表示版本、未知字段、别名候选及依据 |
| Run | 实际输入、代码/环境出处、过程、输出、退出状态、协议一致性、检验资格 |
| Evidence | 数值/观察、单位、误差、目标量、适用域、来源、提取方式、核验记录 |
| HistoricalAssertion | 谁在何时说了什么、当时根据、后续被怎样传递、当前解释 |
| Claim/Assumption | 精确命题与量词、适用范围、支持/反对、采信状态、审查依据 |
| Inference | 联合前提、结论、推导/方法、适用条件、尚缺的连接、核验义务 |
| Branch | 当时问题、解释或方法、证据判定、工作处置、停止原因、重开条件 |
| AuditCase | 需要实际解决的关键关系、材料、分析/检查结果、裁决、影响 |
| Patch/Correction | 原文、修改类型、目标对象、前后版本、支持资格变化、失效视图 |
| Task/Receipt | 目标/类型、read-set、任务产物、权限/资源、实际调用、完成与失败原因 |

首期逐步落地，不为尚无用例的领域强造字段。新科学概念可以以带来源的临时扩展对象进入；晋升到稳定 schema 时必须迁移，不能把旧数据静默重新解释。

## 1.6 可信度采用多轴记录

原件存在、解析正确、运行完成、协议一致、数值收敛、方法适用、推断充分、符合实验，分别记录。对不适用的轴使用 `not_applicable`；对没有检查的轴使用 `unchecked`。不合并为一个可信度总分。

区分三层保证：

- 结构保证：字段、来源定位、版本、权限等由程序检查。
- 检验保证：特定数值/代码/领域条件由对应测试验证。
- 科学裁决：关键推导与解释由有记录的研究者或独立审查给出，可被修订。

## 1.7 原子提交与外部产物

原始输出先写临时区，确认完整后计算指纹并放入不可覆盖的 blob 区。随后在数据库事务中创建引用、更新对象修订、写事件与 outbox。先落 blob 后提交数据库可能留下孤立 blob；恢复工具登记并延迟清理，不能出现数据库指向未完成 blob 的已提交结论。

报告生成使用新版本目录和临时指针；状态提交立即使旧视图过期，渲染失败时继续显示“新视图尚未生成”，不把旧报告当最新。共享引用与负知识保留记录均参与垃圾回收保护。

## 1.8 安全与部署信任边界

建设 agent 是受用户授权修改本项目的开发者。运行中的研究 worker 是权限受限的使用者。两者不同。

R2 的默认动作是只读接入与隔离后处理，不自动运行原仓库脚本、notebook、安装脚本或历史命令。F1 中，worker 不可直接访问数据库写权限、broker 配置、原始凭证、宿主完整文件系统或绕过 broker 的远端终端。

具体隔离方式由 R0/F1 环境检查决定，但隔离义务固定：只读来源挂载、独立可写工作区、受限工具通道、凭证不下发、保护控制代码、实际资源限制。仅依靠 prompts、shell 包装器或同用户可改写的 hooks 时，部署状态只能标作 `observational`，不能声称硬控制已经实现。

威胁模型覆盖误操作、错误工具调用、历史文件中的指令注入和 worker 试图绕过常规流程；不承诺防御宿主管理员或内核漏洞。即便是单用户本机部署，也必须把能产生副作用的 agent 会话置于声明的控制边界中。

## 1.9 建议代码布局

```text
框架/
  docs/                     # 架构与实施包
  src/research_harness/
    contracts/              # 数据对象、接口、迁移
    storage/                # 元数据、blob、事务、outbox
    ingest/                 # 本地/git/导入/只读远端
    parsers/                # 普通格式与领域适配器
    reconstruction/         # 历史、运行、分支、深审、综合
    decisions/              # Jev 与其他语义判断适配
    context/                # 上下文编译与证据按需读取
    runtime/                # F1 的 broker、租约、worker
    governance/             # 修订、权限、人类决策
    views/                  # 阅读视图和交接生成
    cli/                    # CLI / agent tool adapter
  tests/                    # 必要行为与回归
  evaluation/               # E1，独立于生产状态
  config/                   # 不含密钥的部署配置
  var/                      # 运行数据，不提交 git
```

名称可以调整，语义边界与数据职责必须保留。
