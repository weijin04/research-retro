# F1 暂存点

2026-09-20 用户要求：若复盘模块已完整可用，可暂时收口。已在可发布的 R2（`r2-0.2.0`，`2ce434e`）停止继续建设 F1；E1 尚未开始。当前主分支仅保留 R2 产品与交接文档。

F1 草稿完整保存在独立本地分支 `wip/f1-runtime`，未并入发布版：

- `src/research_harness/runtime/broker.py`：任务、租约、持久意图、幂等、父预算、对账及闭环草稿。
- `src/research_harness/runtime/sandbox.py`：bwrap、只读输入、清空环境、seccomp、rlimit 和 tmpfs 配额执行器草稿。
- `tests/test_broker.py`、`tests/test_sandbox.py`：新增测试代码，尚无本轮完整通过回执。
- `research_cases/f1/fc_path_sensitivity/`：正式 TaskProposal、4 个不可变梯度输入身份、动态 Python 程序和推导/输出协议。已校验 schema 与语法，**科学程序未执行、结果未采信**。

实际隔离原型记录在 `var/receipts/f1-sandbox/`：4次简单程序初测中1次输出 `hello` 并封存文件，3次在 namespace 配置或创建阶段失败。它们只证明有限初测情况；未完成越界行为测试、完整资源限制验收或 Broker 接通。

距 F1 发布仍有三组关键工作：

1. 完成 Broker 与执行器/CLI/现有 agent 的集成，核验原件与控制面不可改、无凭证和越权网络访问；完成资源、父预算、幂等、真实进程身份与未知派发对账。需要复核合法科学补丁修改任务读集之后的关闭语义，不能绕过版本检查。
2. 真实执行 FC 路径敏感性任务；由研究者解释输出，经 Workbench 与 ScientificPatch 进入正式状态。执行成功、协议通过与科学支持分别登记。
3. 在克隆状态注入有依据的纠正，证明新上下文和下一步实际行动发生相应变化、无关工作继续；完成服务故障和恢复检查，冻结 F1。之后才进入 E1 系统对比与校准。

因此 F1 是已有原型的下一轮集成与验证工作，不能称为已接近发布或只剩文档。F1 的12项任务目前无一按发布验收标为完成。构建期子代理也遇到了服务使用额度中断；恢复时先确认生成通道可用，不能把中断当作验证通过。

R2 收口检查：`var/receipts/r2_stop_checkpoint.json`，7份冻结回执的哈希全部相符，grephene r2579 与 diffusion r40 的当前状态、发布备份版本和备份完整性一致。已发布代码、运行数据、原件快照和来源定位均保留。源科研文件与原作业未修改。

恢复工作时读取此文及 [当前状态](CURRENT_STATE.md)，再切换 `wip/f1-runtime` 并对照主分支的收口说明；不要把该分支的未验收代码当作 R2 发布内容。
