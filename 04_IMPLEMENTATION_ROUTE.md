# 4. 实现路线与版本退出条件

## 4.1 路线决定

采用“真实用例驱动的纵向贯通 → 完善复盘产品 → 扩展控制闭环 → 集中测评”的顺序。

先把一个真实问题从原件接到科学判定和纠偏，再扩展资产范围、领域和日常可用性。先落数据内核和必要边界，避免为了整个框架预建大量暂时没有调用者的服务。

详细任务见 `06_EXECUTION_CHECKLIST.md` / `backlog.json`。下列顺序决定版本交付，版本内部允许按依赖并行。

## 4.2 总路线图

```text
R0 现场检查、契约、Jev 真实调用
   │
   ├── 来源/存储/定位 ───────┐
   ├── 旧资产导入/解析 ──────┤
   └── Jev 适配/调用记录 ────┤
                            ↓
R1 一个真实 AuditCase 全链路贯通
   原件 → 实际运行 → 主张/推断 → 深审 → 修订 → 报告
                            │
                            ↓
R2 完整复盘产品
   覆盖/异构读取 + 身份/可比性 + 历史/分支/负知识
   + 关键关系深审 + 项目科学地图 + 增量/恢复/交接
                            │
                     R2 可独立发布
                            │
                            ↓
F1 同一内核上的整体科研 MVP
   主持/上下文 → 行动合同 → 受控执行 → 结果核验
   → 科学更新 → 纠偏/重开 → 下一轮行动
                            │
                            ↓
E1 详细测评、Jev 增量归因、修复与复测
```

## 4.3 R0：可工作起点

**成果：**独立项目环境、明确权限与来源、现有资产入口、JevAdapter 的真实调用回执，以及最小交换契约。

优先读取本机已安装 TypeSafe skill。检查实际入口与依赖，不重新安装已有能力。使用公开/合成内容验证 Jev，记录实际模型和 usage；检查失败则修复这一接入，同时推进不依赖 Jev 的存储和读取。

确认 grephene 的旧 census、evidence map、解析脚本与原始文件分布；只做接入范围定位。完整扫描属于后续复盘产品的操作，不在 R0 手工重做全项目研究。

R0 退出时必须能运行一个最小工具命令并留下结构化回执。只有文件夹、类型定义或启动说明不算。

## 4.4 R1：真实纵向链路

**成果：**一份真实、有原件、有科学审计、有状态变化、有交接视图的复盘案例。

从已有 evidence map 挑选一个可访问原件、存在解释或目标量问题、能在只读/轻量后处理中实质解决的承重关系。不要为了容易通过而只选无争议的正例；也不把必须新做昂贵科研计算的问题当唯一启动条件。

同时实现恰好足以支撑该案例的 source snapshot、locator、Run、Claim、Inference、AuditCase、Patch 和报告视图。Jev 以建议模式参与片段分类和关系筛查。

R1 完成后，使用合成复制/撤销案例验证基础版本传播；真实科学判定来自原件和推导。不能把某个旧 reviewer 的结论直接填入产品后宣称发现了该错误。

不在 R1 建设完整图浏览、通用插件商店、复杂集群调度或全项目百种解析器。

## 4.5 R2：首个完善可用产品

**成果：**复盘模块可对声明范围内的真实混乱项目完成任务，并支持恢复、增量、查询和接手。

优先顺序如下：

1. 完成源覆盖、旧索引迁移和异构读取；已存在的解析能力先适配后重写。
2. 建立对象/运行身份、可比性、代码与数据血统，避免把更多错误数据灌进知识图。
3. 恢复五条历史轨迹、分支和负知识，登记孤立结果。
4. 建立可持续分配和执行 AuditCase 的工作台，完成报告所有承重关系的审查。
5. 生成项目科学地图与完整报告，处理纠偏和增量更新。
6. 完善查证浏览、交接、恢复、备份与第二领域切片兼容。

R2 不要求一个模型永久主持全部流程。解析服务、Jev、领域脚本、LLM 深审和人工裁决按任务合作，但必须共享可追溯状态。

**正式交付前的演示：**从初始来源配置恢复项目，回答实际工作/结论资格/失败范围/可复用资产/未解关系；在克隆审计状态中撤销或修正一个承重前提，观察依赖、报告和新交接的变化；再由新上下文 agent 接手查询原件。

R2 发布后才进入整体框架的系统性扩展。不能因为已完成 R1 就转向 F1，留下复盘模块仅有 demo。

## 4.6 F1：整体框架 MVP

**成果：**同一科研状态支撑一个有权限边界、能自选行动、能计算并更新认识、能接受纠偏的研究会话。

复用 R2 的读取、证据、推断、报告和纠偏。新增上下文任务编译、探索/推导/构造/检验等行动合同、资源租约、受控执行、会话恢复、事件触发独立审查和人类入口。

第一条真实运行链使用已有项目数据与有限的后处理/数值检查，避免把框架建设自动变成大规模科研计算授权。受控本地 worker 至少能运行一次真正的程序或数值工作，写入原始输出，并将结果用于下一次行动选择。

需要证明：原始数据不能被 worker 改写；旧前提相关的行动不能在新版本下未经重评就启动；重试不生成重复有副作用任务；Jev 故障不会使安全工作全部停摆；保护配置无法由运行中的研究 worker 改写。

远端 cluster 接口预留。只有当地任务确实需要、已有权限和成熟调度入口时才实现一个代表性真实适配器。没有远端实测的能力标记 `unsupported/unverified`，不填写假成功。

## 4.7 E1：完整测评与定向改进

先冻结版本、题目和测试协议，再比较真实项目重建质量、框架行为、Jev 的独立增量、成本和用户负担。用实际剩余数据、新增数据或来源隔离项目做独立评测；不能把已用于开发的 grephene/diffusion 切片重新命名为 held-out。

测评失效要落实到：候选遗漏、上下文错误、对象混并、数值/实现错误、语义误判、推断不充分、版本传播失败、权限绕过或服务故障，再修复对应层。

E1 的价值包括缩减无收益模块和撤销过度自动化。对照没有证实 Jev 增益时，保留可替换接口，根据任务缩小使用范围，不维护预定结论。

## 4.8 并行与范围控制

存储/事务、解析适配、Jev 适配和视图可以并行，但对外接口及数据含义由一名集成负责人维护。科学深审不能被“等框架全部完工”无限推后，R1 就必须发生。

每轮集成说明三件事：当前版本的可运行产物；尚未解决的关键问题；下一组有依赖依据的任务。不要要求每个 worker 每一步走人工审批。

本包给出功能依赖，没有给日历工期或保证耗时。实施者根据现场资源和真实失败调整顺序，保留 R2 → F1 → E1 的产品优先级。

## 4.9 暂缓事项及重开条件

| 暂缓事项 | 何时重新考虑 |
|---|---|
| 图数据库/微服务 | 单机存储出现实测瓶颈或真实多机写需求 |
| 完整集群编排 | 已有受控本地运行，且具体科研任务需要 |
| 大规模自动多 agent 组织 | 事件触发协作已显现可测收益 |
| 自动科学最终裁决 | 特定领域条件可机器检验且有足够独立证据 |
| 复杂前端 | 查询/查证工作已稳定，现有视图成为真实效率瓶颈 |
| 通用本体 | 多个真实项目显示重复稳定的语义需求 |
| 详细 ablation/校准 | R2、F1 达到可运行发布标准后进入 E1 |
