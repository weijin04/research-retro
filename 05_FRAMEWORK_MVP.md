# 5. 从复盘内核扩展到整体框架 MVP

## 5.1 MVP 的完整闭环

```text
当前目标与证据 → LLM 自选问题/表示/方法
  → 探索或行动提案 → 权限/版本/资源检查
  → 受控工具、领域算法、数值实验
  → 实际输出与协议核验 → 科学推断及修订
  → 更新状态/视图/上下文 → 下一轮行动
```

人类判断、独立重建和反证可以从任何位置进入。原始研究问题无法被当前分类表达时，允许创造临时表示并继续探索；执行所需权限和结果采信义务仍保留。

MVP 覆盖整条运行链，但只承诺已测试的少数执行环境与任务类型。完整多学科工具库和所有集群集成不属于 F1 完成的先决条件。

## 5.2 主持 agent 与现有 cc/codex 环境

先支持一个真实可用的生成模型通道，复用用户现有环境。第二通道在接口稳定后接入。适配器负责派发任务、传递证据包、接收实际工具结果和候选补丁，不依赖未核实的 CLI 选项。

交互模式可以由当前本地 agent 接收 TaskPacket，使用项目工具执行工作并提交结果；受控运行模式由 broker 创建权限受限 worker。前者便于开发与人机协作，后者才可声称被环境强制约束。二者在回执中分别标记。

模型可在授权的探索工作区进行长推导、动态写代码和使用其他工作表示。只在行动提交、科学采信和资源续期处检查契约，不要求输出完整私有推理过程或填固定思考步骤。

## 5.3 行动合同

行动类型至少支持：explore、derive、construct、implement、test、reproduce、repair、maintain。每个行动含目标/问题、类型、read-set、允许动作、预算边界、预期可检查产物和停止/续期条件。

不同类型的完成依据不同：探索可以得到新候选和有依据的空间变化；推导需要真正连接前提与结论；实现需要协议语义与代码一致；数值检验需要原始输出和误差/收敛说明；维护以恢复/覆盖目标验收。不能要求所有行动都立即产生新的科学分支。

连续探索可获得有限资源，不要求在调用工具前预知所有结果。真正昂贵或可能改变路线的检验，应说明合理结果对当前判断和下游行动的影响。

## 5.4 执行状态机

`proposed → admissible → leased → dispatching → running → outputs_sealed → verification_pending → scientific_update_committed → closed`

旁路状态：`rejected`、`paused`、`failed`、`dispatch_unknown`、`stale_reassessment_required`、`cancel_requested`、`cancelled`。进程结束、实验有效、科学更新和任务完成分别登记。

dispatch 前先在数据库事务内创建 attempt、资源占用与幂等键。调用外部执行器后保存其真实 job/process ID。连接断开导致无法确认是否已提交时，进入 `dispatch_unknown`，凭 idempotency key/job tag 查询和对账，禁止盲目再次提交。

对本地可控队列可以保证同一请求键不重复建立任务；跨外部系统不承诺无条件 exactly-once。采用持久意图、幂等接入、对账与失败恢复达成所声明的执行语义。

## 5.5 资源租约

租约绑定 task/attempt、父任务预算、允许工具、输入指纹、工作目录、CPU/GPU/内存/存储、墙钟时间、模型调用和网络范围。租约续期需要展示实际产物及剩余工作；拆成子任务不能重置父任务成本。

资源限额由实际进程隔离/资源控制和调度器实现。无法硬限制的第三方 token/订阅成本明确标为观测或估计，使用可执行的请求数、超时和进程额度限制；不得声称所有成本都已精确受控。

初始研究 smoke 默认不使用 GPU、不提交新集群作业，在独立输出目录进行小型后处理。生产预算由项目授权确定。自报“应该很便宜”不构成无限租约。

## 5.6 权限执行必须能够被行为验证

broker 持有状态写权限和执行凭证；worker 只持有任务所需的短期 capability。token 绑定动作、对象、版本、有效期和可用次数/预算。不可从一个“读取”请求获得任意 shell 权限。

实际部署使用本机可用的进程/容器/用户隔离机制，必须验证：来源只读、可写目录限制、控制配置不可改、无全局凭证、无未授权远端终端、网络外发受到控制。不能把宿主的整套 home、SSH agent、Docker socket 或 unrestricted MCP 直接挂给 worker。

建设者可以修改代码，运行 worker 不可自更新控制面。策略更新先形成版本化部署变更，经过独立于受影响 worker 的审批/校验再启用。

隔离能力缺失时继续完成复盘和其他可运行部件，记录具体能力缺口。不能把 advisory/hooks 模式的成功演示标成 enforceable MVP。

## 5.7 上下文编译与漂移恢复

ContextPacket 包含：目标合同版本、有效纠正、当前对象定义、承重证据和相应反证、必要负知识、竞争模型与未知、当前任务、读取入口。输出带 mandatory/optional、来源 ID、版本与选择日志。

上下文超预算时，优先分阶段阅读、拆任务或使用索引继续获取；不静默删除有效纠正和反证。低成本检索/分类不能单独决定“不符合主叙事的材料”不进入系统。

会话结束或压缩后，从正式状态重新编译上下文。旧 agent 总结只是历史视图；每个新任务的 read-set 可以让系统判定它是否读了过期前提。

## 5.8 纠偏事务与支持集合

每个推断表示为联合前提集合；一个结论可由多个独立或相关支持集合获得支持。

例：`C ← (E1 ∧ A)` 或 `(E2 ∧ B)`。撤销 A 时重算第一组；第二组仍有效则保留 C 的相应支持。没有剩余支持表示 `unsupported`，不会推出 C 为假。明确反证另记 contradiction/refutation 关系与适用范围。

纠偏先提交目标对象和必然影响的失效标记，再异步扩展隐含关系。尚未完成语义扫描时记录 `impact_scan_pending`；被明确命中的高影响操作先要求重评，无关工作继续。

并发提交携带对象级 read-set 和策略版本。系统只拒绝相关依赖冲突，避免全局版本号变化让所有任务失效。过期任务的原始结果仍入库到当时的输入身份，科学采信需要重新解释。

## 5.9 防路径依赖与计算错配的最小机制

不按固定轮数强制换方向。以下事件可以触发 clean-context 重建：核心前提撤销；多个异质异常长期未被解释；表示无法容纳必要候选；同类任务反复续期但未满足声明产物；重要决策依赖高度相关的证据。

触发的后果必须具体：重新编译独立证据包、保留替代候选、重新比较任务、改变或续期资源分配。只增加一段“反思”日志没有完成事件处理。

LLM 可以主动提出重开表示，不依赖 Jev 正确识别。独立审查在看到 lead 的结论前先完成自己的问题重建；分歧保留，禁止投票把多模型输出计为多份物理证据。

## 5.10 人类入口

立即执行已有授权覆盖的常规可逆动作。人类介入聚焦目标/证据标准变更、额外权限和预算、不可逆操作、真实科学取舍，以及系统保护失效。

上报包包含原始证据入口、冲突、各选项的后果、仍可并行做的工作。接受“暂时不依赖这个假设”后应直接改变使用资格；是否物理上为假另需证据。

## 5.11 F1 的一次端到端演示

从 R2 的真实审计结果进入新会话。主持者选择一个仍有价值且在授权内可推进的关系，提出有限后处理或数值检验；broker 真实执行，记录原始结果；研究者完成解释并修订状态；在克隆状态上注入一次有依据的前提变化；下一轮上下文和任务选择体现该变化，同时保留不受影响的工作。

必须报告完成的科学推断和实际动作，不仅展示队列与仪表板。该演示证明受控科研循环运行，不等于所有化学机制已闭合或自主科研能力已得到统计证明。
